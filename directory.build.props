<Project>

  <Import Project="z0.props"/>
  <Import Project="$(Src)symbols.props"/>
  <Import Project="$(Test)symbols.props"/>
  <Import Project="$(Native)symbols.props"/>

  <PropertyGroup>  
    <Parts>$(TopDir)parts/z0.parts.csproj</Parts>
  </PropertyGroup>
  
</Project>
